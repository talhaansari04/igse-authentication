name: CI

on:
  push:
    branches:
      - 'feature*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Environment Setup
        run: |
          echo "Running integration tests..."
  tag:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Development Tag Creation
        run: echo "Creating development tag..."