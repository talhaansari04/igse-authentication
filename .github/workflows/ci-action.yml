name: igse-ci
on:
  push:
    branches:
      - develop
      - feature/*
  pull_request:
    branches:
      - develop

jobs:
  build:
    uses: talhaansari04/igse-ci/.github/workflows/ci.yml@develop
    with:
      java-version: '17'
      service-name: 'igse-auth-service'
