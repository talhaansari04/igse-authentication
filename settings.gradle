pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/milestone' }
		maven { url 'https://repo.spring.io/snapshot' }
		mavenLocal()
		gradlePluginPortal()
	}
}
rootProject.name = 'igse-authentication'
include 'performance'