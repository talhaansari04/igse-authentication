#spring:
#  kafka:
#    bootstrap-servers: localhost:9092

services:
  igse:
    core:
      endpoint: http://localhost:8080/igse-core
    payment:
      endpoint: http://localhost:8080/igse-payment
  kafka:
    regisTopics: v1.dev.regi
    groupId: "default-dev"

service:
  kafka:
    enable: true
    wallet:
      topic: "wallet.v1"
      groupId: "wallet-dev-A1"
poller:
  outOfBox:
    cron: "0 0/1 * * * ?"
    enable: true
spring:
  jpa:
    show-sql: false
  sql:
    init:
      mode: ALWAYS
      data-locations:
        - classpath:sql/cleanUp.sql
        - classpath:sql/dashboard_admin.sql